# Loopback smoke: do not consume public auth rate limits (VPS verify only).
geo $souq_loopback_smoke {
    default 0;
    127.0.0.1 1;
}

map $souq_loopback_smoke $souq_api_auth_limit_key {
    0 $binary_remote_addr;
    1 "";
}
